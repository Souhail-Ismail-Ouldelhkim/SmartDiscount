# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: smart-discount
services:  
  app:
    language: dotnet
    project: ./SmartDiscount.AppHost/SmartDiscount.AppHost.csproj
    host: containerapp
